<!DOCTYPE html>
<html>
   <body>
    <h1>Today</h1>
      <% weekday = Date.today.strftime("%A") %>
      <% month_and_day = Date.today.strftime("%B %d") %>
      <% year = Date.today.strftime("%Y") %>
      <p> <%= "The date is #{weekday}, #{month_and_day}, #{year}." %></p>
   </body>
</html>